"""Слой analyze: ЕДИНСТВЕННЫЙ слой, где допустим вызов LLM.

Превращает детерминированные метрики (data/metrics/) и качественные входы
(inputs/) в черновики находок findings/draft/*.yaml. Черновики обязательно
проходят ручную проверку аналитиком: утверждённые переносятся в
findings/approved/ (гейт перед слоем report).
"""
